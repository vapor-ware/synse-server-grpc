#!/usr/bin/env groovy

// Include this shared CI repository to load script helpers and libraries.
library identifier: 'vapor@1.0.1', retriever: modernSCM([
  $class: 'GitSCMSource',
  remote: 'https://github.com/vapor-ware/ci-shared.git',
  credentialsId: 'vio-bot-gh',
])

pythonPipeline([
  'pythonVersion': '3.8',
  'mainBranch': 'master',
  'skipSetup': true,
  'skipLint': true,
  'skipUnitTest': true,
  'skipIntegrationTest': true,
  'skipDocker': true,
  'publishToGithub': true,
  'publishToPypi': true,
  'twineCredential': 'twine-password',
])
